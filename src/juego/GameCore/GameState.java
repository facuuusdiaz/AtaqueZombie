package juego.GameCore;

/**
 * Representa en qué pantalla/estado está el juego en un momento dado.
 */
public enum GameState {
    MENU,
    INSTRUCTIONS,
    OPTIONS,
    PLAYING,
    PAUSED,
    GAME_OVER,
    WON
}
